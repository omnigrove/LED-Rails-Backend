apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "LED-Rails-Backend.fullname" . }}-test-connection"
  labels:
    {{- include "LED-Rails-Backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "LED-Rails-Backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
