apiVersion: v1
kind: Service
metadata:
  name: {{ include "LED-Rails-Backend.fullname" . }}
  labels:
    {{- include "LED-Rails-Backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "LED-Rails-Backend.selectorLabels" . | nindent 4 }}
